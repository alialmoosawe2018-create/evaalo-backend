version: '3.8'

services:
  custom-llm-server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - DEBUG=False
      - MODEL_NAME=custom-llm
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped

